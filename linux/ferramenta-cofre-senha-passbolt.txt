# Entrar no container do passbolt

# Passo 02 - Executar o comando abaixo
su -s /bin/bash -c "/usr/share/php/passbolt/bin/cake passbolt register_user -i
" www-data

# Copiar e colar no navegador a URL gerada